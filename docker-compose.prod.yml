version: "3.8"

services:
  bot:
    restart: always
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    command: yarn start
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
